<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Aniversario65anos">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".FotosActivity"
            android:label="Mural de Lembranças"
            android:parentActivityName=".MainActivity"/>

        <activity
            android:name=".MenuActivity"
            android:label="Menu de Planejamento"
            android:parentActivityName=".MainActivity"/>

        <activity
            android:name=".ConvidadosActivity"
            android:label="Lista de Convidados"
            android:parentActivityName=".MenuActivity"/>

        <activity
            android:name=".GastosActivity"
            android:label="Controle de Gastos"
            android:parentActivityName=".MenuActivity"/>

        <activity
            android:name=".CalculoActivity"
            android:label="Cálculo de Festa"
            android:parentActivityName=".MenuActivity"/>

    </application>

</manifest>