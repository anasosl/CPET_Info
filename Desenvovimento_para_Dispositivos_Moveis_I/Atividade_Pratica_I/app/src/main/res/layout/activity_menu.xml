<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="20dp"
    android:background="#FDF5E6">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/myToolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="#FDF5E6"
        app:layout_constraintTop_toTopOf="parent"
        app:titleTextColor="?attr/colorPrimary" />

    <Button
        android:id="@+id/btnConvidados"
        android:layout_width="0dp"
        android:layout_height="70dp"
        android:text="Lista de Convidados"
        android:layout_marginTop="30dp"
        app:layout_constraintTop_toBottomOf="@id/myToolbar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <Button
        android:id="@+id/btnGastos"
        android:layout_width="0dp"
        android:layout_height="70dp"
        android:text="Controle de Gastos"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/btnConvidados"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <Button
        android:id="@+id/btnCalculo"
        android:layout_width="0dp"
        android:layout_height="70dp"
        android:text="Cálculo de Comes e Bebes"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@id/btnGastos"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>